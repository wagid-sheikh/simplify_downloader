# Example environment configuration for Simplify Downloader
# Copy to downloader/.env and fill in real credentials before running the pipeline.

TD_BASE_URL="https://simplifytumbledry.in"
TD_HOME_URL="https://simplifytumbledry.in/home"
TD_LOGIN_URL="https://simplifytumbledry.in/home/login"
# TD_BASE_URL="https://tms.simplifytumbledry.in"  # Alternate base URL if required
TMS_BASE="https://tms.simplifytumbledry.in"
TD_STORE_DASHBOARD_PATH="/mis/partner_dashboard?store_code={store_code}"

# Store UN3668 credentials
TD_UN3668_USERNAME="your_un3668_username"
TD_UN3668_PASSWORD="your_un3668_password"
TD_UN3668_STORE_CODE="UN3668"

# Store KN3817 credentials
TD_KN3817_USERNAME="your_kn3817_username"
TD_KN3817_PASSWORD="your_kn3817_password"
TD_KN3817_STORE_CODE="KN3817"

# Optional: override the default store selection used by the CLI (comma separated)
# STORES_LIST="UN3668,KN3817"

# Database connection for ingestion (asyncpg DSN)
DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/simplify"

# Batch size for ingestion upserts (defaults to 3000 if unset)
# INGEST_BATCH_SIZE="3000"

# Structured JSON logs can be duplicated to a file if desired
# JSON_LOG_FILE="/var/log/simplify_downloader.jsonl"

# Location of Alembic configuration (usually no change required)
# ALEMBIC_CONFIG="alembic.ini"
